<main>
  <h1>New comment</h1>
  <%= form_with model: Comment, url: user_post_comments_path, method: :post, local: true do |f| %>
    <div>
      <%= f.text_area :text, placeholder: "Add your comment" %>
    </div>
    <%= f.submit "Comment" %>
  <% end %>
</main>